{
  "name": "devsynq",
  "version": "1.0.0",
  "description": "AI IDE Launcher - Launch your favorite AI-powered development environments",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "build": "bun run build:renderer && bun run build:main && bun run build:preload",
    "build:main": "bun build ./src/main.ts --outfile=./dist/main.js --target=node --format=cjs --external electron",
    "build:preload": "bun build ./src/preload.ts --outfile=./dist/preload.js --target=node --format=cjs --external electron",
    "build:renderer": "vite build",
    "start": "bun run build && electron .",
    "dev": "bun run build && electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "build": {
    "appId": "com.devsynq.idelauncher",
    "productName": "DevSynq",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/icon.ico"
    },
    "mac": {
      "target": "dmg",
      "icon": "assets/icon.icns"
    },
    "linux": {
      "target": "AppImage",
      "icon": "assets/icon.png"
    }
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.10.1",
    "electron": "^39.2.6",
    "electron-builder": "^26.0.12"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "lucide-react": "^0.556.0",
    "react": "^19.2.1",
    "react-dom": "^19.2.1",
    "vite": "^7.2.7"
  }
}